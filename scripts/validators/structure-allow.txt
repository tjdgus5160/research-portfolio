# Deliberate structural changes, one per line, exactly as the gate reports them.
# Anything not written here fails the check.
#
# The five service symbols were geometric SVG shapes. The author asked for
# photographs instead. The <svg> wrapper is kept — styles.css binds the hover
# rotate/scale to `.service-visual svg`, and replacing the element outright
# would have silently dropped that motion — so only its children changed, from
# drawn shapes to a single <image>. That alters the node count, which is more
# than an entry-per-line list can express, so index.html's baseline moves to the
# commit that introduces it (see structure-base.txt).
#
# styles.css and script.js are still compared against the original adoption
# commit and are still byte-identical. Nothing has ever been allowed to touch
# the files that carry the design.

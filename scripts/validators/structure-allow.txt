# Deliberate structural changes, one per line, exactly as the gate
# reports them. Anything not written here fails the check.

# The hero image is no longer a glass letter W. It is a render of an Rb
# vapor cell, generated locally with krea2 at the same 1536x1024 as the
# original, so nothing about the layout changes.
<img width='1536' height='1024' src='assets/glass-w.png' -> <img width='1536' height='1024' src='assets/vapor-cell.png'
<img width='1536' height='1024' src='assets/glass-w.png' -> <img width='1536' height='1024' src='assets/vapor-cell.png'
